import { Routes } from '@angular/router';
import { TreeComponent } from './components/tree/tree.component';

export const routes: Routes = [
    {path: './app/components/tree/tree.component.ts', component: TreeComponent},
];
